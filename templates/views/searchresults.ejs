<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
    <%- include ("../partials/csslinker")%>
</head>
<body>
    <%- include ("../partials/navbar")%>
    <div class="container" height="100%" style="margin-top:15vh">
        <% if(data.length>0) { %>
            <% for (var i = 0; i < data.length;  i++) { %>
                <div class="row ml-auto mr-auto" style="height:25vh;width:100%;margin: 20px; color: black;">
                    <div class="col-md-3 col-xs-3 col-sm-3">
                        <img src="/books/<%=data[i]._id%>/bookcover" height="70%" width="70%">
                    </div>
                    <div class="col-md-9 col-xs-9 col-sm-9" style="height:100%;">
                        
                        <table style="text-align: left;">
                            <tr>
                                <td><strong>Book Name:</strong> </td>
                                <td><%= data[i].bookname %></td>
                            </tr>
                            <tr>
                                <td><strong>Quantity:</strong></td>
                                <td><%= data[i].quantity %></td>
                            </tr>
                            <tr>
                                <td><strong>description:</strong></td>
                                <td><%= data[i].description %></td>
                            </tr>
                            <tr>
                                <td><strong>Donor Address:</strong></td>
                                <td><%= data[i].address %></td>
                            </tr>
                            <tr>
                                <td><strong>Donor Email:</strong></td>
                                <td><%= data[i].useremail%></td>
                            </tr>
                            <tr>
                                <td><strong>Send Request</strong></td>
                                <td><form action="/books/request/<%=data[i]._id %>" method="post"> <input type="text" name="reason" placeholder="Mention Why do you need this book?" style="width:60%"> <button  class="btn btn-primary btn-xs" type="submit">REQUEST</button></form><a  href=""></td>
                            </tr>
                        </table>
                    </div>
                    
                </div>
            <% } %> 
            <% } else { %>
                <div style="height:25vh;width:100%;margin: 20px; color: black;">
                    <h3>No Search results found</h3>
                </div>
            <% } %>
    </div>

    <%- include ("../partials/jslinker") %>
</body>
</html>
